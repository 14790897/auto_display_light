{
  "branches": ["main"],
  "tagFormat": "v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/changelog",
      {
        "changelogFile": "CHANGELOG.md"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "firmware_output/firmware.factory.bin",
            "label": "firmware.factory.bin - 首次烧录固件（含 bootloader）"
          },
          {
            "path": "firmware_output/firmware.bin",
            "label": "firmware.bin - 固件（烧录到 0x10000）"
          },
          {
            "path": "firmware_output/firmware.ota.bin",
            "label": "firmware.ota.bin - OTA 升级固件"
          }
        ]
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md", "package.json"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ]
  ]
}
